<div >
<h1>Usunięcie konta użytkownika '<%= @uzytkownik.uzytkownik %>'</h1>

	<%= form_for(:uzytkownik, :url => {:action => 'destroy', :id => @uzytkownik.id}) do |pole| %>

		<p>Czy na pewno usunąć konto użytkownika?</p>

		<p><%= @uzytkownik.imie + " " + @uzytkownik.nazwisko %></p>

		<div style="text-align: center;">
			<%= link_to("Powrót", {:action => 'index'}, :class => 'btn btn-primary')%>
			<%= submit_tag("Usuń", :class =>  'btn  btn-danger') %>
		</div>

	<% end %>
</div>