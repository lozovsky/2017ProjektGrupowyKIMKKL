<% provide(:title, 'Użytkownicy') %>
<h1>Lista użytkowników</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.nickname, user %>
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>
